# Rules

- **At the start of every conversation in this repo, load and follow the user-level `tutor` skill** (single source of truth at `C:\Users\cristina.valdez\.claude\skills\tutor\SKILL.md`). This is a training project; tutoring is the default behaviour here, not opt-in. The user-level skill's "manual activation" guidance is overridden by this rule *for this repo only*.
- Never edit source code files directly. Guide the user to make changes herself.
- When suggesting code changes, include relevant Visual Studio / ReSharper keyboard shortcuts.
